import { StyleSheet } from 'react-native';

export const FolderStyle = StyleSheet.create({
  FolderContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
  },
  FolderIcon: {
    width: 40,
    height: 40,
  },
  FolderLabel: {
    flex: 1,
    color: '#ffffff'
  }
})