import { StyleSheet } from 'react-native';

export const FolderStyle = StyleSheet.create({
  FolderContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    minWidth: 70
  },
  FolderIcon: {
    width: 60,
    height: 40,
  },
  FolderLabel: {
    flex: 1,
    color: '#ffffff'
  }
})